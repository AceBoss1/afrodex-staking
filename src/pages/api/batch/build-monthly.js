// Placeholder API route — real logic will be added later when Supabase is ready.
export default function handler(req, res) {
  return res.status(200).json({ message: "Batch monthly placeholder OK" });
}
